meta:
  name: stress-test
  cluster: kafka-dev
  environment: dev
  region: us-east-1

spec:
  partitions: 9
  replicationFactor: 3
  placement:
    strategy: any
  settings:
    retention.ms: "86400000"       # 1 day
    cleanup.policy: "delete"
    min.insync.replicas: "2"
